{
    "name": "devops_api",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "start": "node src/server.js",
        "health": "node -e \"require('http').get('http://localhost:'+(process.env.PORT||5000)+'/health',res=>{console.log('status',res.statusCode);process.exit(res.statusCode===200?0:1)}).on('error',()=>process.exit(1))\"",
        "test": "node test/basic.test.js",
        "lint": "echo \"No lint configured for api\""
    },

    "dependencies": {
        "express": "^4.21.2",
        "mysql": "^2.18.1",
        "mysql2": "^3.10.2",
        "node-uuid": "1.4.7",
        "pg": "^8.16.3"
    }
}