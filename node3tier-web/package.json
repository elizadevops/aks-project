{
  "name": "node3tier-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "vite build",
    "preview": "vite preview",
    "start": "serve -s dist -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.3"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
